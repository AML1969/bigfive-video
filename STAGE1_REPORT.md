# Этап 1. Big Five по видео на готовых весах — отчёт

Дата: 2026-09-14 → 2026-09-15 (ночь). Статус: **этап завершён**, все замеры на полном test FIV2 выполнены.

## 0. Главное

| Система (все веса публичные) | mACC, test 1997 клипов | mCCC | Время на 15-с клип |
| --- | ---: | ---: | ---: |
| OCEAN-AI (аудио + видео + текст) | 0.9255 | 0.703 | 2.4–3.3 с |
| SSL-MEPR, только ветвь «сцена» | 0.9135 | 0.670 | 0.8 с |
| **Ансамбль: среднее OCEAN-AI и сцены (`--backend ensemble`)** | **0.9256** | **0.720** | ~4 с |
| Ориентиры из статей: MM-PSYCHE single-task SL / OCEAN-AI paper | 0.918 / 0.9245 | 0.740 / — | — |

Реальный режим «на входе только видео» (транскрипт от Whisper large-v3-turbo) стоит 0.3 п.п. mACC: 0.921 против
0.924 с профессиональными транскриптами на 200 клипах. Команда `bs infer video.mp4 --out result.json` работает в WSL.

## 1. Что сделано

| Шаг | Результат |
| --- | --- |
| Поиск весов SSL-MEPR | текст, аудио и сцена скачаны (`~/bs/ssl_mepr_ckpt/`); лицо и тело авторы не опубликовали (папка Drive `models_checkpoints` пуста и под аккаунтом, зеркал нет) |
| Альтернатива | найден **OCEAN-AI** (aimclub/OCEANAI, BSD-3) тех же авторов: полный пайплайн аудио+видео+текст для Big Five, все веса публичные (FIV2 и MuPTA) |
| Окружение | WSL2 Ubuntu 24.04, `~/bs/venv`: torch 2.11.0+cu130 (RTX 5090 Laptop, sm_120), oceanai 1.0.0a48, transformers 4.45.1, mediapipe 0.10.21, numpy 1.26.4. Скрипт `bs/scripts/setup_wsl.sh` |
| Пакет `bs` | `BS/bs/` — CLI `bs infer / infer-dir / eval-fiv2 / setup-weights`, два бэкенда (`oceanai`, `sslmepr`), JSON-отчёт по схеме ТЗ с перцентилями относительно train FIV2 |
| Данные | все 10 000 клипов FIV2 скачаны другой сессией в `BS/data/first-impressions-v2/`; для замеров собраны `~/bs/eval/fi_test200` (200 клипов) и `~/bs/eval/fi_test_all` (2000 клипов test) с транскриптами Rev и метками |

## 2. Точность OCEAN-AI (веса `fi`, транскрипты Rev, без ASR)

200 клипов test (`bs/results/oceanai_fi_test200.json`):

| Черта | ACC = 1−MAE | CCC | Pearson |
| --- | ---: | ---: | ---: |
| Открытость | 0.925 | 0.651 | 0.713 |
| Добросовестность | 0.925 | 0.737 | 0.813 |
| Экстраверсия | 0.929 | 0.714 | 0.773 |
| Доброжелательность | 0.915 | 0.566 | 0.671 |
| Эмоц. стабильность | 0.924 | 0.716 | 0.800 |
| **Среднее** | **0.924** | **0.677** | — |

Заявленное авторами на полном test: mACC 0.9245. Для сравнения MM-PSYCHE single-task SL: mACC 0.918, CCC 0.740.
Разброс предсказаний заметно уже разброса меток (sd 0.08–0.10 против 0.14–0.16): модель тянет к среднему, поэтому
CCC ниже, чем у MM-PSYCHE, при равном mACC.

Полный test (`bs/results/oceanai_fi_test_all.json`): обработано 1997 из 2000 клипов, 3 клипа библиотека отбросила
(`JmAQlC-FEV8.000`, `L-rmZZP_wj8.005`, `_plk5k7PBEg.004`). Проверено: у всех трёх в FIV2 пустой транскрипт — в ролике
нет речи, а трёхмодальный фьюжн OCEAN-AI без текста файл пропускает. Итог **mACC 0.9255, mCCC 0.703**, 3.3 с на клип
при параллельном прогоне. Заявленные авторами 0.9245 воспроизведены.

Известное ограничение: видео без речи (или с пустым результатом Whisper) сейчас даёт ошибку «no predictions».
В OCEAN-AI есть отдельный аудио-видео фьюжн без текста (`get_av_union_predictions`, веса `av/fi` скачаны);
подключить его как запасной путь — задача следующего шага.

| Черта | ACC | CCC | Pearson |
| --- | ---: | ---: | ---: |
| Открытость | 0.922 | 0.667 | 0.740 |
| Добросовестность | 0.926 | 0.732 | 0.788 |
| Экстраверсия | 0.930 | 0.756 | 0.810 |
| Доброжелательность | 0.924 | 0.616 | 0.695 |
| Эмоц. стабильность | 0.926 | 0.742 | 0.807 |

Режим «только видео на входе» (транскрипт от Whisper large-v3-turbo через transformers внутри OCEAN-AI) проверен на
одном клипе: транскрипт близок к Rev, оценки отличаются на 0.01–0.02 (`bs/results/smoke_asr.json`). Первый клип
16 с из-за загрузки Whisper (1.6 ГБ), далее ожидается ~4–6 с на клип.

Замер с ASR на тех же 200 клипах (`bs/results/oceanai_fi_test200_asr.json`): **mACC 0.921, mCCC 0.664** против
0.924 / 0.677 с транскриптами Rev. То есть реальный пайплайн «только видео» теряет 0.3 п.п. mACC. Время 7.4 с на
клип, но замер шёл параллельно с двумя другими прогонами; в одиночку ожидается ~4 с.

## 3. Скорость (RTX 5090 Laptop, WSL)

| Операция | Время |
| --- | ---: |
| Загрузка всех моделей OCEAN-AI (fi) | 30 с (Whisper ещё +10 с при первом ASR) |
| OCEAN-AI, один 15-с клип, без ASR | 2.4 с (одиночный прогон) — 3.3 с (параллельно с другим замером) |
| OCEAN-AI, один клип с Whisper-транскриптом | ~4 с в одиночку; 7.4 с при трёх параллельных прогонах |
| SSL-MEPR, ветвь «сцена», один клип | 0.8 с |
| Ансамбль, один клип | ~4 с без ASR |
| OCEAN-AI, 2000 клипов | 6523 с (1 ч 49 мин) |

Нагрузка в основном на CPU (MediaPipe FaceMesh, openSMILE), GPU занята слабо: параллельные прогоны упираются
в 24 потока процессора, а не в видеокарту.

## 4. SSL-MEPR на трёх модальностях (сцена + аудио + текст)

Smoke-тест прошёл (загрузка 9 с, 4 с на клип), но выдал важный факт: опубликованный fusion-чекпоинт MCDM обучался на
пяти модальностях и без лица и тела **схлопывается** — все пять оценок 0.11–0.19 для клипа с метками 0.42–0.66.
Унимодальные же предсказания разумны (сцена 0.39–0.55, текст 0.42–0.50, аудио 0.27–0.36). Поэтому в бэкенде
`sslmepr` основной оценкой сделано позднее слияние — среднее трёх унимодальных предсказаний, а выход MCDM и каждая
модальность отдельно сохраняются как варианты и тоже оцениваются.

Замер на тех же 200 клипах test (`bs/results/sslmepr_fi_test200.json`), 1.7 с на клип:

| Вариант SSL-MEPR | mACC | mCCC |
| --- | ---: | ---: |
| Позднее слияние: среднее сцена+аудио+текст (основной выход) | 0.913 | 0.579 |
| Только сцена (CLIP по всему кадру + Transformer/Mamba) | **0.915** | **0.704** |
| Только аудио (wav2vec2 + Mamba) | 0.901 | 0.547 |
| Только текст (BGE-small + Transformer) | 0.874 | 0.137 |
| MCDM-фьюжн без лица и тела | 0.865 | 0.577 |
| OCEAN-AI, три модальности, для сравнения | 0.924 | 0.677 |

Вывод: одна ветвь «сцена» SSL-MEPR даёт CCC выше, чем весь OCEAN-AI, хотя mACC у OCEAN-AI выше. Текстовая ветвь
на транскриптах почти не несёт сигнала (CCC 0.14). Это подсказка для этапа 2: визуальная ветвь с CLIP — главный
источник качества, а слияние стоит обучать самим.

### Ансамбль двух систем (`scripts/ensemble_check.py`, те же 200 клипов)

| Комбинация | mACC | mCCC |
| --- | ---: | ---: |
| OCEAN-AI | 0.924 | 0.677 |
| SSL-MEPR, сцена | 0.915 | 0.704 |
| **Среднее OCEAN-AI и сцены SSL-MEPR (равные веса)** | **0.926** | **0.730** |
| Среднее OCEAN-AI, сцены и аудио SSL-MEPR | 0.925 | 0.707 |

Простое усреднение двух готовых систем даёт CCC 0.730 — на уровне MM-PSYCHE single-task (0.740) при более
высокой mACC. Веса не подбирались (равные), чтобы не подстраиваться под test. Добавлен бэкенд `--backend ensemble`.

На всех 2000 клипах test ветвь «сцена» SSL-MEPR даёт mACC 0.9135, mCCC 0.671 (0.78 с на клип), то есть выборка из
200 клипов была для неё оптимистичной (`bs/results/sslmepr_fi_test_all.json`).

Ансамбль на полном test, 1997 общих клипов (`bs/results/ensemble_fi_test_all.json`):

| Комбинация | mACC | mCCC | CCC: O / C / E / A / N-N |
| --- | ---: | ---: | --- |
| OCEAN-AI | 0.9255 | 0.703 | 0.667 / 0.732 / 0.756 / 0.616 / 0.742 |
| SSL-MEPR, сцена | 0.9135 | 0.670 | 0.641 / 0.758 / 0.720 / 0.563 / 0.671 |
| **Среднее, равные веса (бэкенд `ensemble`)** | **0.9256** | **0.720** | 0.686 / 0.785 / 0.775 / 0.618 / 0.738 |
| 0.6 OCEAN-AI + 0.4 сцена | 0.9267 | 0.723 | 0.688 / 0.781 / 0.778 / 0.623 / 0.745 |

Прирост CCC от ансамбля на полном test +0.017 (на выборке из 200 был +0.053). Равные веса оставлены по умолчанию;
вариант 0.6/0.4 чуть лучше, но подобран на test и потому не принимается без проверки на dev.

## 5. Как пользоваться

```bash
# из WSL
~/bs/venv/bin/bs infer /mnt/c/path/video.mp4 --out result.json          # английская речь, Whisper turbo
~/bs/venv/bin/bs infer video.mp4 --lang ru --out result.json              # русская речь: веса MuPTA + перевод
~/bs/venv/bin/bs infer video.mp4 --transcript video.txt --out result.json # свой транскрипт вместо ASR
~/bs/venv/bin/bs infer-dir ./videos --out results.csv
```

Пример результата: `bs/results/smoke_oceanai.json`.

## 6. Найденные особенности OCEAN-AI (учтены в обёртке)

- Корень датасета должен содержать подпапки с файлами (`depth=1` перечисляет подкаталоги), поэтому обёртка подкладывает
  временный корень с папкой `clips/`.
- Пути символических ссылок разворачиваются, транскрипт `<stem>.txt` ищется рядом с настоящим файлом; при
  `--transcript` клип жёстко линкуется/копируется во временную папку под безопасным именем.
- ffmpeg вызывается shell-строкой без кавычек: пробелы в имени файла ломают запуск, поэтому имя санитизируется.
- Рядом с видео создаётся `<stem>.wav` (44.1 кГц, стерео).
- Библиотека сравнивает имя корпуса через `is`, поэтому строки интернируются (`sys.intern`).
- `transformers==4.45.1` жёстко; `torchaudio` импортируется на уровне модуля, поэтому взят torch 2.11 — последняя
  версия с парным torchaudio под cu130.
- В ASR-пути библиотека зовёт `torchaudio.load`, который в torchaudio ≥ 2.9 требует `torchcodec`; обёртка подменяет
  его загрузчиком на `soundfile` с тем же контрактом.
- Whisper внутри OCEAN-AI грузится через transformers в fp32; модель задаётся `--asr-model` (по умолчанию
  `openai/whisper-large-v3-turbo`).

## 7. Что дальше (этап 2, по ТЗ)

1. Собственное обучение по рецепту MM-PSYCHE на FIV2 (данные уже локально): CLIP + CLAP + EmoRoBERTa (+ описание
   поведения через Ollama `qwen3-vl:30b` или `qwen2.5vl:7b`) → MCDM только для personality. Цель: CCC ≥ 0.72 при mACC ≥ 0.918.
2. Сравнить три системы на одном и том же test: OCEAN-AI, SSL-MEPR (3 модальности), своя модель.
3. Веб-интерфейс поверх лучшего бэкенда.
